<main class="giveaway-page">
  <div class="giveaway-intro">
    <h1>{{ page.title }}</h1>
    {%- if page.content != blank -%}
      <div>{{ page.content }}</div>
    {%- endif -%}
  </div>
</main>

{% schema %}
{
  "name": "Page",
  "settings": []
}
{% endschema %}
